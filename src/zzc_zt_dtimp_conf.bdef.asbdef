projection;
strict ( 2 );
use draft;

define behavior for ZZC_ZT_DTIMP_CONF alias Configuration
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}